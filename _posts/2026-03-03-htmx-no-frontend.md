---
layout: post
title: "HTMX: когда лучший фронтенд — это его отсутствие"
date: 2026-03-03
---

POV: Вы бэкендер. Вам бы хотелось написать админку для вашего сервиса. Ну не через сваггер же методы вызывать в 2026 году?

## Ищем: как сделать фронтенд легко?

- npm npx tailwind nodejs react vue angular solidjs
- пытаемся сделать простой проект
- десятки непонятных конфигов в которых путаются LLMки потому что спецификации меняются оч часто
- ???
- сдаёмся

Это не тривиальные инструменты)) Надо +- понимать что происходит внутри процесс билда итд. Возникает вопрос, ради чего мы тратим весь этот mental overhead? Нужны лишь таблички с CRUD операциями💀

Давайте явно посмотрим на путь данных, отраженный в коде:

- SQL или ORM
- backend сервис
- REST handler
- JSON
- сеть
- десериализация
- state
- props
- компонент
- HTML

Вот бы как-то что-то упростить) Глобально направления два: убрать часть бэкенда (BaaS) либо убрать часть фронтенда... и сегодняшний пост пропагандирует именно это - ВООБЩЕ не писать отдельно фронтенд а использовать так называемый HTMX.

Что же это за зверь и какие у него идеи? Разработчик htmx предлагает нам... вместо json в эндпоинтах возвращать html💀💀 и в тэгах указывать какую часть DOM дерева мы хотим заменить.

## Давайте конкретный пример

```html
<button hx-get="/users"
        hx-target="#table"
        hx-swap="innerHTML">
  Загрузить
</button>
```

Переводится на русский язык как:

- При нажатии на кнопку.
- Браузер отправляет GET-запрос на /users.
- Сервер возвращает HTML.
- Этот HTML вставился внутрь элемента с id="table", полностью заменив его содержимое.

Путь данных в таком случае:

- SQL или ORM
- backend
- HTML

Решение позволяет убрать пять этажей инфраструктуры) Иногда лучший фронтенд - это отсутствие фронтенда.

Подключение библиотеки тривиально тем что это так называемый 0-build.
Никакого npm, vite, webpack.

Один тег в <head>:

```html
<script src="https://unpkg.com/htmx.org@1.9.12"></script>
```

Это обычный HTML-документ. Стоит ли тут что-то добавлять? Кода сильно меньше...

## Когда об этой технологии стоит задуматься

- Админки, внутренние панели
- Дашборды
- MVP в которых важнее показать что продукт работает, а не то как он выглядит

А вот когда точно тащить его не стоит:

- Сложный клиентский state
- Реалтайм с тонкой синхронизацией (хотя htmx умеет в SSE)
- Canvas, графические редакторы
- Большие SPA с автономной логикой на клиенте
- Когда фронтенд команда уже существует

Ни один профессиональный фронтендер не захочет поддерживать эту шляпу)) Им это не нужно в резюме, это не industry standard итд

И вот вы спросите... это что получается теперь для создания любой логики в браузере придётся писать эндпоинт на бэкенде?? Абсолютная руина. А я вам отвечу, коллеги!!! Обязательно отвечу. Но в следующих постах. А пока я с вашей стороны ТРЕБУЮ лайков и распространения этих знаний. Отправьте этот пост друзьям и заставьте из подписаться на этот канал. Здесь говорят правду.
