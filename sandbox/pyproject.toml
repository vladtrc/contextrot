[project]
name = "sandbox"
version = "0.1.0"
description = "Unified UV sandbox for post experiments and generated visuals."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.8",
    "reportlab>=4.4.10",
]
